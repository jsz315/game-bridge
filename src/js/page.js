import {getHtmlByData} from './tooler'
import './pop/index'

var html = getHtmlByData("rank-none", {});
// console.log(html);

// GAME.tool.httpGet("/youtui/share/commonShareUrl", {}, (rsp)=>{})